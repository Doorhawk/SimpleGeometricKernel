#pragma once
enum class DependsTypes {
    None,
    Parallel,
    Perpendicular,
    MedianPerpendicular,
    BelongsToLine,
    IntersectionLineLine
};

enum class wMode {
    cameraMove,
    figureMove
};